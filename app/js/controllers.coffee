'use strict'

window.FirstCtrl = ($scope, $http) ->
	$scope.hello = "Hello World"

# FirstCtrl.$inject = [$scope, $http]

window.SecondCtrl = ($scope, $http) ->
	$scope.hello = "Hello World"

# SecondCtrl.$inject = [$scope, $http]